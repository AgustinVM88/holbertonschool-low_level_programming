#include "main.h"

/**
 * add - function that adds two integers and returns the result.
 * @a: first int to add
 * @b: second int to add
 *
 * Return: result(a + b).
 */
int add(int a, int b)
{
	return (a + b);
}
